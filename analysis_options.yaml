analyzer:
  exclude:
    # Generated test stubs - not shipped, not worth linting.
    - test/**
  errors:
    # The generated client carries imports/params the analyzer flags as unused;
    # they are harmless. Silence so `dart analyze` and pub.dev scoring stay clean.
    unused_import: ignore
    unused_element_parameter: ignore
